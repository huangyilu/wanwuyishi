node_modules/
dist/
.DS_Store
*.local
.env
.env.*
!.env.example

# 构建产物：由 content/ 经 scripts/build-index.ts 生成，不手改也不提交
public/data/

# 瓦片/打包构建工具的本地缓存（JDK 等），非前端源码
.tilebuild/

# 项目本地记忆/工作区元数据（含内部讨论，不进公网仓库）
.workbuddy/

# Agent CLI（scripts/wwys-cli.ts）缓存的登录 session，含 access/refresh token
.wwys-session.json

# 构建缓存（TypeScript / Vite 临时产物，非源码）
*.tsbuildinfo
vite.config.ts.timestamp-*.mjs
