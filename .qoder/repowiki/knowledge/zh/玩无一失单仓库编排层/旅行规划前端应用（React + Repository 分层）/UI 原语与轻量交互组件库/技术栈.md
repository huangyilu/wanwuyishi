React 函数组件 + CSS Modules + CSS Custom Properties（设计 token）；弹窗类组件统一使用 `react-dom/createPortal` 挂载至 `document.body`；PDF 预览通过 Supabase Storage 客户端下载为 Blob 并用 `application/pdf` MIME 类型渲染；SVG 内联绘制，零第三方图标库。