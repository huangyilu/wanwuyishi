`scripts/build-pmtiles.sh` 可独立运行，首次执行会自动下载 JDK 21、pmtiles 与 Planetiler 到 `.tilebuild/`；支持 `MAXZOOM=13 ./scripts/build-pmtiles.sh` 调整细节，`--force` 强制重新下载缓存；产物需小于 100MB 才能推送到 GitHub Pages。`scripts/verify-supabase.cjs` 与 `scripts/verify-password-login.cjs` 需要项目根目录存在 `.env` 且包含 `VITE_SUPABASE_URL` 与 `VITE_SUPABASE_ANON_KEY`。