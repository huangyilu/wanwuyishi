运行前需执行 `npm run content:build` 生成 `public/data/index.json` 等静态世界库资源，否则 `StaticJsonWorldRepository` 会报资源缺失；云端模式需在 `.env` 中配置 `VITE_SUPABASE_URL` 与 `VITE_SUPABASE_ANON_KEY`，并在 Supabase 后台开启 Anonymous sign-ins 或 Email provider。