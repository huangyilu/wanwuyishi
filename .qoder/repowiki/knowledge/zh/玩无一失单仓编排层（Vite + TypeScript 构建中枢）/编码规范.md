- 所有子模块共享根 `tsconfig.json` 的 `@/*` 路径别名与严格类型检查规则（strict/noUnusedLocals/noUncheckedIndexedAccess）
- 构建产物使用相对 base 路径以便在 GitHub Pages 子路径与离线 file:// 协议下均可运行
- 通过 npm scripts 组合 content:validate → content:build → tsc → vitest → vite build 的固定顺序保证数据源、类型与测试一致性