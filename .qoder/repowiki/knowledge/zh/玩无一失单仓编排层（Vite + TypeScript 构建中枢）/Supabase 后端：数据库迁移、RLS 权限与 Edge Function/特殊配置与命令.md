部署 Edge Function：`supabase functions deploy chat-proxy`，并通过 `supabase secrets set DEEPSEEK_API_KEY=sk-xxxx` 注入密钥；数据库结构通过 `migrations/*.sql` 顺序执行，附件能力需手动在 Supabase SQL Editor 中运行 `apply_attachments.sql`（因无 service_role key，无法随 CLI 自动应用）。