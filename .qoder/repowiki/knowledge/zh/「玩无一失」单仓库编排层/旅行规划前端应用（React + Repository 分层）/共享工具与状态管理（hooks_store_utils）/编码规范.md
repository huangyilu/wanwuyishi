- 跨页面持久化统一通过带命名前缀的 key（如 `wwys:*`）读写 `localStorage` / `sessionStorage`，并在读取时做 try/catch 或类型校验以兼容脏数据。
- 状态变更采用「先 set/get 新值，再调用独立 persist 函数」的模式，将渲染更新与磁盘落盘解耦。
- 全局可观察状态以模块级变量 + listeners Set 的形式维护，并通过自定义 hook 暴露给 React 组件订阅。
- 对外暴露的纯工具函数保持无副作用、参数即返回值的设计，如 `downloadTextFile`、`safeFileName`。